<?xml version="1.0"?>
<!--
  Unraid Community Applications template for StavenMediaManager.

  To install manually on Unraid (without CA):
    SSH into Unraid and run:
    cd /boot/config/plugins/dockerMan/templates-user/
    wget https://raw.githubusercontent.com/BluPhant/StavenMediaManager/main/StavenMediaManager.xml

  To add to Community Applications:
    Apps tab → Settings → "Add template repository" →
    BluPhant/StavenMediaManager
-->
<Container version="2">
  <Name>StavenMediaManager</Name>
  <Repository>ghcr.io/BluPhant/stavenmediamanager:latest</Repository>
  <Registry>https://github.com/BluPhant/StavenMediaManager/pkgs/container/stavenmediamanager</Registry>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/BluPhant/StavenMediaManager/issues</Support>
  <Overview>
    Local media management tool for Unraid.&#xA;&#xA;
    Enumerates incoming directories by media type (movies, audiobooks, music, games, etc.),
    detects RAR archives, and extracts them with real-time async progress tracking.
    All data is stored in a single SQLite file inside the appdata volume — no separate
    database container required.
  </Overview>
  <Category>MediaApp: Tools:</Category>
  <WebUI>http://[IP]:[PORT:8088]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/BluPhant/StavenMediaManager/main/StavenMediaManager.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/BluPhant/StavenMediaManager/main/unraid/icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <!-- ── Ports ─────────────────────────────────────────────────────────── -->
  <Config
    Name="WebUI Port"
    Target="8080"
    Default="8080"
    Mode="tcp"
    Description="Port the web interface listens on."
    Type="Port"
    Display="always"
    Required="true"
    Mask="false">8088</Config>

  <!-- ── Volumes ───────────────────────────────────────────────────────── -->
  <Config
    Name="Appdata (Config &amp; Database)"
    Target="/config"
    Default="/mnt/user/appdata/StavenMediaManager"
    Mode="rw"
    Description="Persistent storage for the SQLite database and application settings. Back this up to preserve job history."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/appdata/StavenMediaManager</Config>

  <Config
    Name="Incoming Directory"
    Target="/incoming"
    Default="/mnt/user/incoming"
    Mode="rw"
    Description="Parent directory whose subdirectories are treated as media categories (e.g. /mnt/user/media-files/temp/incoming contains Movies/, Audiobooks/, Music/). Read-write so extracted files can be placed in-place."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false">/mnt/user/media-files/temp/incoming</Config>

  <Config
    Name="TMDb API Key"
    Target="TMDB_API_KEY"
    Default=""
    Mode=""
    Description="Free API key from https://www.themoviedb.org/settings/api — required for IMDB movie matching. Leave blank to disable the feature."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="Media Library"
    Target="/media"
    Default="/mnt/user/media"
    Mode="rw"
    Description="Destination media library root. Workflow actions will move processed content here (e.g. movies land in {Media Library}/movies/{Title (Year)}/)."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false">/mnt/user/media-files</Config>

  <!-- ── rTorrent / Seedbox Sync (optional) ──────────────────────────────── -->
  <Config
    Name="rTorrent XMLRPC URL"
    Target="RTORRENT_URL"
    Default=""
    Mode=""
    Description="XMLRPC endpoint for your rTorrent instance, e.g. https://username.servername.usbx.me/RPC2"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="rTorrent Username"
    Target="RTORRENT_USER"
    Default=""
    Mode=""
    Description="HTTP Basic Auth username for the XMLRPC endpoint."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="rTorrent Password"
    Target="RTORRENT_PASS"
    Default=""
    Mode=""
    Description="HTTP Basic Auth password for the XMLRPC endpoint."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="rTorrent Import Tag"
    Target="RTORRENT_TAG"
    Default="import"
    Mode=""
    Description="ruTorrent label to watch for importing (e.g. 'import'). After download the label is renamed to '{tag}-imported'."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">import</Config>

  <Config
    Name="rTorrent Lookback Hours"
    Target="RTORRENT_LOOKBACK_HOURS"
    Default="24"
    Mode=""
    Description="How many hours back to check for completed torrents."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">24</Config>

  <Config
    Name="Seedbox SSH Host"
    Target="RTORRENT_SSH_HOST"
    Default=""
    Mode=""
    Description="SSH hostname for file download, e.g. servername.usbx.me"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Seedbox SSH User"
    Target="RTORRENT_SSH_USER"
    Default=""
    Mode=""
    Description="SSH username (usually the same as your rTorrent username)."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Seedbox SSH Password"
    Target="RTORRENT_SSH_PASS"
    Default=""
    Mode=""
    Description="SSH password. Leave blank if using an SSH key instead."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="Seedbox SSH Key Path"
    Target="RTORRENT_SSH_KEY_PATH"
    Default="/config/ssh/id_rsa"
    Mode=""
    Description="Path inside the container to your SSH private key. Mount the key file as a volume at this path."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">/config/ssh/id_rsa</Config>

  <!-- ── FTPS Download (replaces SFTP — faster, multi-threaded) ─────────────── -->
  <Config
    Name="Seedbox FTP Host"
    Target="RTORRENT_FTP_HOST"
    Default=""
    Mode=""
    Description="FTP host for seedbox downloads, e.g. 216.163.184.165 or servername.usbx.me. Uses FTPS (FTP over TLS) on port 21. Falls back to RTORRENT_SSH_HOST if blank."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Seedbox FTP Port"
    Target="RTORRENT_FTP_PORT"
    Default="21"
    Mode=""
    Description="FTPS port — default 21."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">21</Config>

  <Config
    Name="Seedbox FTP Root"
    Target="RTORRENT_FTP_ROOT"
    Default=""
    Mode=""
    Description="Absolute path on the server that is the FTP root, e.g. /home/emuhack. Defaults to /home/{username} if blank."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Seedbox FTP Threads"
    Target="RTORRENT_FTP_THREADS"
    Default="4"
    Mode=""
    Description="Number of parallel FTPS connections per torrent download (default 4 — matches FileZilla multi-thread performance)."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">4</Config>

  <!-- ── IPTorrents Search (optional) ───────────────────────────────────── -->
  <Config
    Name="IPTorrents User ID"
    Target="IPTORRENTS_USER_ID"
    Default=""
    Mode=""
    Description="Your numeric IPTorrents user ID — shown on your profile page. Required to use the Search feature."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="IPTorrents Passkey"
    Target="IPTORRENTS_PASSKEY"
    Default=""
    Mode=""
    Description="Your IPTorrents passkey / RSS API key — found on your profile page under RSS."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="IPTorrents Domain"
    Target="IPTORRENTS_DOMAIN"
    Default="iptorrents.com"
    Mode=""
    Description="IPTorrents domain — override if using a mirror. Default: iptorrents.com"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false">iptorrents.com</Config>

  <Config
    Name="BTN API Key"
    Target="BTN_API_KEY"
    Default=""
    Mode=""
    Description="BroadcasTheNet API key — from your BTN profile under Manage API Keys. Enables TV search via BTN."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

  <Config
    Name="Plex URL"
    Target="PLEX_URL"
    Default=""
    Mode=""
    Description="Base URL of your Plex server, e.g. http://192.168.1.x:32400 — used to trigger a library refresh after a move. Leave blank to disable."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"></Config>

  <Config
    Name="Plex Token"
    Target="PLEX_TOKEN"
    Default=""
    Mode=""
    Description="Your Plex authentication token (X-Plex-Token). Find it at https://support.plex.tv/articles/204059436"
    Type="Variable"
    Display="always"
    Required="false"
    Mask="true"></Config>

</Container>
