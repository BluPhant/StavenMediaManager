# Normalize line endings to LF in the repo and on checkout
# (Docker, Python, and shell scripts require LF)
* text=auto eol=lf

# Windows-only files can keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
